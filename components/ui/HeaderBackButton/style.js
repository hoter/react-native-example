import createStyles from '../../../styles/';
import { colors, fonts } from '../../../styles/base';

export default createStyles({
  icon: {
    color: colors.base,
    fontSize: fonts.slg - 4,
    fontWeight: 'bold',
    textAlign: 'left',
  }
});
